#ifndef PALINDROME_H
#define PALINDROME_H

#include <stdlib.h>
#include <stdio.h>

/* Checks whether or not a given unsigned integer is a palindrome*/
int is_palindrome(unsigned long n);

#endif /* PALINDROME_H */
